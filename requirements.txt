# =============================================================================
# requirements.txt
# CT Liver Tumour Diffusion Pipeline
#
# Install with:
#     pip install -r requirements.txt
#
# PyTorch is NOT listed here because the correct version depends on your
# CUDA version.  Install it first from https://pytorch.org/get-started/locally/
# before running pip install -r requirements.txt.
#
# Example (CUDA 12.1):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Example (CPU only):
#     pip install torch torchvision
# =============================================================================


# ── Core scientific stack ─────────────────────────────────────────────────────
numpy>=1.24,<3.0
opencv-python>=4.8
matplotlib>=3.7
tqdm>=4.65
pandas>=2.0

# ── Medical imaging ───────────────────────────────────────────────────────────
nibabel>=5.2            # read .nii / .nii.gz NIfTI files

# ── Deep learning utilities ───────────────────────────────────────────────────
einops>=0.7             # used in models.py (AttentionBlock rearrange)

# ── LaMa inpainting via ONNX (no Pillow / Python-version constraints) ─────────
# Works on Python 3.12, 3.13, 3.14+
onnxruntime-gpu>=1.17   # GPU inference  — remove and use line below if no GPU
# onnxruntime>=1.17     # CPU-only alternative — uncomment if no CUDA GPU
huggingface_hub>=0.22   # one-time download of lama_fp32.onnx (~208 MB)